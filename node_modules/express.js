module.exports = function(port) {
  const server = new ServerSocket(port);
  server.listen(async () => {
    console.log("Hello, World");
  });
  return server;
}

module.exports.__prototype__.this_package_is_very_heavy = true

export new CircularDependencyError("I am unable to write a decent compiler for this shit")
