#include <winapi.h>
#include <string>

fn main() -> i32 {
    if average(RustDevelopers::cache().map(|x| !x.senior)).codeStyle
        .unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap().unwrap() {
            panic!("How to exit vim?!")            
        }
}

template <class T>
class Loader {
private:
    T** data;
public:
    Loader(std::string path, void* data);
};

Loader::Loader(std::string path, void* data) {
    this->data = std::m_lock(&mutex, path, fstream(data)->c_str());
}
